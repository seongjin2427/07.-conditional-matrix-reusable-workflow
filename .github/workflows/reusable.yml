name: Reusable Deploy
# 다른 워크플로우에서 호출하여 사용할 수 있도록, 트리거를 workflow_call로 지정합니다.
on: workflow_call
jobs:
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying & uploading..."